version: '3.4'services:  mariano-store--sql-server:    image: mcr.microsoft.com/mssql/server:2022-latest    container_name: mariano-store--sql-server    ports:      - "14336:1433"    environment:      - MSSQL_SA_PASSWORD=yourStrong(!)Password      - ACCEPT_EULA=Y    volumes:      - 'mariano-store--sql_data:/var/opt/mssql/data'      - 'mariano-store--sql_log:/var/opt/mssql/log'    networks:      - mariano-store-network  mariano-store--rabbitmq:    container_name: mariano-store--rabbitmq    hostname: rabbitmq    build:      context: ./rabbitmq    volumes:      - mariano-store--rabbitmq_data:/var/lib/rabbitmq/mnesia    ports:      - "15674:15670"      - "5674:5670"           healthcheck:      test:        [          "CMD-SHELL",          "curl -I -s -L http://localhost:15670 || exit 1"        ]      interval: 30s      timeout: 20s      retries: 5    networks:      - mariano-store-networkvolumes:  mariano-store--rabbitmq_data:  mariano-store--sql_data:    external: true  mariano-store--sql_log:    external: true